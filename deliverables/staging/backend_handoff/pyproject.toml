[project]
name = "vehicle-memory-demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "lightmem",
  "openai",
  "pydantic",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-p no:benchmark"
